Museum : ok

Floor :
	//ajouter texuture pour le sol = generateGround()
	//ajouter éclairage = generateLight()
	//ajouter les données correspondantes dans le json : ground texture, éclairages
	//gérer position de l'étage / hauteur de l'étage

Wall :
	ajouter fenetres = generateWindows()
	ajouter peintures = generatePaintings()
	régler hauteur des portes/fenetres/tableaux

Model : 
	ajouter model = generate()

Update Library



2D:
	- faire le drag des walls _ inprogress